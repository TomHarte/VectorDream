@cars: EQU FOR NUM_CARS
car{@cars}:
@cardef:
	db -128					; Current x position.
	db colour.car.blue		; Car colour.
	dw 5					; Forward velocity.

	jp car{@cars}.collide
	jp car{@cars}.update
car{@cars}.draw:
	ld a, (@-cardef+1)
	ld iyl, a
	ld a, (@-cardef)
	jp draw.car
NEXT @cars
